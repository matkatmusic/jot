[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
markers = [
    "live: tests that exercise real external systems (tmux, filesystem, network) rather than mocks",
]
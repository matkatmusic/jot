Basename index:
audit_bare_refs.py: /Users/matkatmusicllc/Programming/jot/audit/audit_bare_refs.py
build_call_graph.py: /Users/matkatmusicllc/Programming/jot/audit/build_call_graph.py
audit_gen.py: /Users/matkatmusicllc/Programming/jot/audit/audit_gen.py
jot_plugin_orchestrator.py: /Users/matkatmusicllc/Programming/jot/scripts/jot_plugin_orchestrator.py
capture-conversation.py: /Users/matkatmusicllc/Programming/jot/skills/jot/scripts/capture-conversation.py
format_open_todos.py: /Users/matkatmusicllc/Programming/jot/skills/todo-list/scripts/format_open_todos.py
hookjson_lib.py: /Users/matkatmusicllc/Programming/jot/common/scripts/hookjson_lib.py
git_test_funcs_lib.py: /Users/matkatmusicllc/Programming/jot/common/scripts/git_test_funcs_lib.py
jot_lib.py: /Users/matkatmusicllc/Programming/jot/common/scripts/jot_lib.py
util_lib.py: /Users/matkatmusicllc/Programming/jot/common/scripts/util_lib.py
tmux_lib.py: /Users/matkatmusicllc/Programming/jot/common/scripts/tmux_lib.py
git_lib.py: /Users/matkatmusicllc/Programming/jot/common/scripts/git_lib.py
debate_lib.py: /Users/matkatmusicllc/Programming/jot/common/scripts/debate_lib.py
todo_lib.py: /Users/matkatmusicllc/Programming/jot/common/scripts/todo_lib.py
plate_dispatcher.py: /Users/matkatmusicllc/Programming/jot/common/scripts/plate_dispatcher.py
claude_lib.py: /Users/matkatmusicllc/Programming/jot/common/scripts/claude_lib.py
bg_permissions_lib.py: /Users/matkatmusicllc/Programming/jot/common/scripts/bg_permissions_lib.py
strip_stdin.py: /Users/matkatmusicllc/Programming/jot/common/scripts/jot/strip_stdin.py
expand_permissions.py: /Users/matkatmusicllc/Programming/jot/common/scripts/jot/expand_permissions.py
render_template.py: /Users/matkatmusicllc/Programming/jot/common/scripts/jot/render_template.py
clear_drift_alert.py: /Users/matkatmusicllc/Programming/jot/common/scripts/plate/clear_drift_alert.py
check_live_children.py: /Users/matkatmusicllc/Programming/jot/common/scripts/plate/check_live_children.py
verify_stash_refs.py: /Users/matkatmusicllc/Programming/jot/common/scripts/plate/verify_stash_refs.py
instance_rw.py: /Users/matkatmusicllc/Programming/jot/common/scripts/plate/instance_rw.py
spawn_summary_agent.py: /Users/matkatmusicllc/Programming/jot/common/scripts/plate/spawn_summary_agent.py
transcript_parse.py: /Users/matkatmusicllc/Programming/jot/common/scripts/plate/transcript_parse.py
plate_cli.py: /Users/matkatmusicllc/Programming/jot/common/scripts/plate/plate_cli.py
register_parent.py: /Users/matkatmusicllc/Programming/jot/common/scripts/plate/register_parent.py
check_drift_alert.py: /Users/matkatmusicllc/Programming/jot/common/scripts/plate/check_drift_alert.py
_rebase_reword_summary.py: /Users/matkatmusicllc/Programming/jot/common/scripts/plate/_rebase_reword_summary.py
cascade_parent_chain.py: /Users/matkatmusicllc/Programming/jot/common/scripts/plate/cascade_parent_chain.py
append_plate_to_stack.py: /Users/matkatmusicllc/Programming/jot/common/scripts/plate/append_plate_to_stack.py
next_resume_point.py: /Users/matkatmusicllc/Programming/jot/common/scripts/plate/next_resume_point.py
render_tree.py: /Users/matkatmusicllc/Programming/jot/common/scripts/plate/render_tree.py
commit_message.py: /Users/matkatmusicllc/Programming/jot/common/scripts/plate/commit_message.py
print_resume_pointer.py: /Users/matkatmusicllc/Programming/jot/common/scripts/plate/print_resume_pointer.py
list_paused_plates.py: /Users/matkatmusicllc/Programming/jot/common/scripts/plate/list_paused_plates.py
plate_lib.py: /Users/matkatmusicllc/Programming/jot/common/scripts/plate/plate_lib.py
check_rolling_intent_refresh.py: /Users/matkatmusicllc/Programming/jot/common/scripts/plate/check_rolling_intent_refresh.py
# Call graph

Generated by `audit/build_call_graph.py` on 2026-05-11.

Entry point: `dispatch_main()` in `scripts/jot_plugin_orchestrator.py`.

Legend:
- `[blocking subproc]` = `subprocess.run` / `check_output` / etc. (parent waits)
- `[FaF subproc]`      = `subprocess.Popen` (parent does not wait — orphan risk)
- `(↑ seen: <fn>)`     = recursion guard (function already on the call stack)
- `-> return N`        = early-exit annotation (call is followed by `return N`)

---

## Tree

```
dispatch_main()
  if argv is None:
  if argv:
    via _ARGV_DISPATCH:
      jot_sessionStart()  # _ARGV_DISPATCH['jot-session-start']
        if not input_file or not tmpdir_inv: -> return 0
        for _ in range(5):
          try:
            if target_file.is_file() and target_file.stat().st_size > 0:
              if first_line:
          except OSError:
        if not tmux_target: -> return 0
        if tmux_waitForClaudeReadiness(tmux_target) != 0: -> return 1
        tmux_sendAndSubmit(tmux_target, f'Read {input_file} and follow the instructions at the top of that file') -> return 0
          tmux_sendKeys(pane_target, text)
            [blocking subproc] <unresolved argv>
            if result.returncode != 0:
          if rc1 != 0:
      jot_sessionEnd()  # _ARGV_DISPATCH['jot-session-end']
        if not tmpdir_inv: -> return 0
        if not re.match('^(/tmp/jot\\.|/private/tmp/jot\\.)', tmpdir_inv): -> return 0
      jot_stop()  # _ARGV_DISPATCH['jot-stop']
        if not input_file or not tmpdir_inv or (not state_dir): -> return 0
        _jot_readSidecar(target_file)
          for _ in range(5): -> return ''
            try:
              if target_file.is_file() and target_file.stat().st_size > 0:
                if first_line:
            except OSError:
        if not tmux_target: -> return 0
        jot_initState(state_dir)
          for name in ('queue.txt', 'active_job.txt', 'audit.log'):
        _util_isoTimestampLocal()
        if input_path.is_file():
          try:
          except OSError:
          if first_line.startswith('PROCESSED:'):
            _util_appendAudit(audit_path, f'{ts} SUCCESS {input_file}')
          else:
            _util_appendAudit(audit_path, f'{ts} FAIL {input_file} (no PROCESSED marker)')
        else:
          _util_appendAudit(audit_path, f'{ts} FAIL {input_file} (input.txt missing)')
        jot_rotateAudit(audit_path, 1000) -> return 0
          if not path.is_file(): -> return None
          if line_count <= max_lines: -> return None
          try:
          except BaseException: -> return None
            try:
            except FileNotFoundError:
      todo_scanOpen()  # _ARGV_DISPATCH['scan-open-todos']
        if not todos_dir.is_dir():
        for md_path in sorted(todos_dir.glob('*.md')):
          if not md_path.is_file():
          if _todo_has_open_status(md_path):
      todo_launcher()  # _ARGV_DISPATCH['todo-launcher']
        if not session_id: -> return 1
        if not idea: -> return 1
        if not pending_file_path: -> return 1
        if not pending_file.is_file(): -> return 1
        try:
        except Exception:
        try:
        except Exception: -> return 1
        _util_hide_errors(git_getBranchNameOrFail, Path(cwd))
          try:
          except Exception: -> return '(unavailable)'
        _util_hide_errors(git_getRecentCommitHashes, Path(cwd))
          try:
          except Exception: -> return '(unavailable)'
        _util_hide_errors(git_getUncommittedFilenames, Path(cwd))
          try:
          except Exception: -> return '(unavailable)'
        _util_hide_errors(todo_scanOpen, repo_root)
          try:
          except Exception: -> return '(unavailable)'
        if transcript_path and Path(transcript_path).is_file():
          try:
            skills/jot/scripts/capture-conversation.py::main() [blocking subproc]
              (no resolvable entry function in target)
          except subprocess.CalledProcessError:
        else:
        try:
          common/scripts/jot/render_template.py::main() [blocking subproc]
            (no resolvable entry function in target)
        except Exception:
        bgPermissions_loadClaude('todo')
          _loadSection(tool, 'claude', bundle_path, log_file)
            _seedIfNeeded(bundle_path)
              _resolveInstalled()
                if not plugin_data:
              _resolveBundle(bundle_path)
                if bundle_path is not None:
                if not plugin_root:
              claude_seedPermissions(str(installed_path), str(bundle_json), str(bundle_sha), str(prior_sha_path), log_file, 'bg_agent_permissions')
                if not Path(default).is_file() or not Path(default_sha_file).is_file():
                  claude_permseedLog(f'bundled permissions default missing at {default} - cannot seed', log_file, log_prefix)
                    if not log_file:
                    try:
                    except OSError:
                _util_readFirstToken(default_sha_file)
                if not Path(installed).is_file():
                  claude_permseedLog(f'seeded {installed} from bundled default (sha={current_default_sha})', log_file, log_prefix)
                    if not log_file:
                    try:
                    except OSError:
                try:
                  _util_sha256File(installed)
                except OSError:
                _util_readFirstToken(prior_sha_file)
                if installed_sha == current_default_sha:
                if prior_sha and installed_sha == prior_sha:
                  claude_permseedLog(f'upgraded {installed} to new bundled default (was {prior_sha}, now {current_default_sha})', log_file, log_prefix)
                    if not log_file:
                    try:
                    except OSError:
                if prior_sha != current_default_sha:
                  claude_permseedLog(f'{installed} is user-edited; bundled default updated - diff manually. installed_sha={installed_sha} prior_sha={prior_sha} current_default_sha={current_default_sha}', log_file, log_prefix)
                    if not log_file:
                    try:
                    except OSError:
            try:
            except (OSError, json.JSONDecodeError):
              _resolveBundle(bundle_path)
                if bundle_path is not None:
                if not plugin_root:
            if key not in data:
            if worker not in section:
          _substituteTemplates(section.get('allow', []), env)
            for item in items:
          if extra_allow:
        claude_buildCmd(str(settings_file), allow_json, str(hooks_json_file), cwd, repo_root)
          for extra in extra_dirs:
        try:
        except Exception: -> return 1
        terminal_spawnIfNeeded('todo', log_file, 'todo') -> return 0
          if not session:
          _terminal_listTmuxClients(session)
            try:
              tmux list-clients [blocking subproc]
              if result.returncode != 0: -> return ''
            except (OSError, FileNotFoundError): -> return ''
          if clients.strip(): -> return 0
          if sys.platform == 'darwin':
            if shutil.which('osascript') is None:
              _terminal_appendAdvisory(log_file, log_prefix, session) -> return 0
                if not log_file or log_file == '/dev/null':
                try:
                except OSError:
            _terminal_buildOsascript(session, maximize)
            try:
              osascript [FaF subproc]
              try:
              finally:
            except (OSError, subprocess.SubprocessError): -> return 0
          _terminal_appendNonDarwinAdvisory(log_file, log_prefix, session) -> return 0
            if not log_file or log_file == '/dev/null':
            try:
            except OSError:
      todo_stop()  # _ARGV_DISPATCH['todo-stop']
        if not input_file or not tmpdir_inv or (not state_dir): -> return 0
        for _ in range(_SIDECAR_RETRIES):
          if target_file.is_file() and target_file.stat().st_size > 0:
            if first and first[0].strip():
        if not tmux_target: -> return 0
        if inp.is_file():
          if first_line.startswith('PROCESSED:'):
            try:
            except FileNotFoundError:
          else:
        else:
        jot_rotateAudit(audit_path, _AUDIT_MAX_LINES) -> return 0
          if not path.is_file(): -> return None
          if line_count <= max_lines: -> return None
          try:
          except BaseException: -> return None
            try:
            except FileNotFoundError:
      todo_sessionStart()  # _ARGV_DISPATCH['todo-session-start']
        if not input_file or not tmpdir_inv: -> return 0
        for _ in range(_POLL_ATTEMPTS):
          if target_file.is_file() and target_file.stat().st_size > 0:
            if first_line:
        if not tmux_target: -> return 0
        if tmux_waitForClaudeReadiness(tmux_target) != 0: -> return 1
      todo_sessionEnd()  # _ARGV_DISPATCH['todo-session-end']
        if not any((tmpdir_inv.startswith(prefix) for prefix in _VALID_PREFIXES)):
      plate_summaryStop()  # _ARGV_DISPATCH['plate-summary-stop']
        if not repo or not branch or (not output_file): -> return 0
        if not Path(output_file).is_file(): -> return 0
        if plate_log_env:
        if Path(repo).is_dir():
        else:
        try:
        except OSError:
        try:
          common/scripts/plate/plate_cli.py::main() [blocking subproc]
            _cmd_set_plate_summary() [via plate_cli.py argv 'set-plate-summary']
              if len(argv) != 3: -> return 'plate: usage: set-plate-summary <repo> <branch> <summary-file>'
              plate_regenerateTipSummary(repo, branch)
                _parseAgentPayload(new_payload)
                  if not text.strip():
                  if blank_idx is None:
                if not body:
                run(['git', 'rev-parse', plate_branch])
                  if env is not None:
                  [blocking subproc] <unresolved argv>
                  if not capture: -> return ''
                run(['git', 'log', '-1', '--format=%B', tip_sha])
                  if env is not None:
                  [blocking subproc] <unresolved argv>
                  if not capture: -> return ''
                run(['git', 'rev-parse', f'{tip_sha}^{{tree}}'])
                  if env is not None:
                  [blocking subproc] <unresolved argv>
                  if not capture: -> return ''
                run(['git', 'rev-parse', f'{tip_sha}~1'])
                  if env is not None:
                  [blocking subproc] <unresolved argv>
                  if not capture: -> return ''
                _stripSummaryTrailerFromMessage(tip_msg)
                if subject:
                  _replaceCommitSubject(cleaned, subject)
                    if len(new_subject) > 50:
                    if not new_subject:
                    for (i, line) in enumerate(lines):
                      if stripped and (not stripped.startswith('#')):
                    if message.endswith('\n') and (not rebuilt.endswith('\n')):
                _appendSummaryTrailerToMessage(cleaned, trailer_body)
                  while content and content[-1].strip() == '':
                  _format_trailer_body(summary)
                    if not raw: -> return ''
                  if comments:
                  if not rebuilt.endswith('\n'):
                run(['git', 'commit-tree', tip_tree, '-p', tip_parent, COMMIT_MESSAGE_FLAG, new_msg])
                  if env is not None:
                  [blocking subproc] <unresolved argv>
                  if not capture: -> return ''
                run(['git', 'update-ref', f'refs/heads/{plate_branch}', new_tip_sha])
                  if env is not None:
                  [blocking subproc] <unresolved argv>
                  if not capture: -> return ''
        except Exception:
        try:
        except OSError: -> return 0
      plate_summaryWatch()  # _ARGV_DISPATCH['plate-summary-watch']
        if timeout is None:
        if interval is None:
        while elapsed < timeout: -> return 1
          if _ready():
            try:
            except Exception:
            try:
            except Exception: -> return 0
      debate_tmuxOrchestrator()  # _ARGV_DISPATCH['debate-tmux-orchestrator']
        if not session:
        if not resolved_agents:
        try:
        finally: -> return 0
      jot_collectDiagnostics()  # _ARGV_DISPATCH['jot-diag-collect']
        if out_path is None:
        try:
          git -C [blocking subproc]
        except FileNotFoundError:
        jot_diagSection('1. Latest Todos/*_input.txt')
        _util_ls_latest_input_txt(todos_dir)
        if latest is None:
        else:
          jot_diagKv('path', str(latest))
          try:
          except OSError:
          jot_diagKv('size (bytes)', str(size))
          try:
          except OSError:
          jot_diagKv('mtime', mtime)
          try:
          except OSError:
          jot_diagKv('first line', first_line)
          if first_line.startswith('PROCESSED:'):
            jot_diagKv('status', 'PROCESSED (success)')
          if first_line == '# Jot Task':
            jot_diagKv('status', "PENDING (claude hasn't finished OR failed)")
          else:
            jot_diagKv('status', '? unknown first-line format')
        jot_diagSection(f'2. State dir ({state_dir})')
        if not state_dir.is_dir():
        else:
          try:
            ls -la [blocking subproc]
          except FileNotFoundError:
          jot_diagIndent(ls_out)
            if not text:
          if queue_file.exists():
            if queue_file.stat().st_size > 0:
              jot_diagIndent(queue_file.read_text(errors='replace'))
                if not text:
            else:
          else:
          if active_file.exists():
            if active_file.stat().st_size > 0:
              jot_diagIndent(active_file.read_text(errors='replace'))
                if not text:
            else:
          else:
          if audit_file.exists():
            jot_diagIndent(_util_tail_lines(audit_file, 30))
              if not text:
            _util_tail_lines(audit_file, 30)
              try:
              except OSError: -> return ''
          else:
          if lock_path.exists() or lock_path.is_symlink():
          else:
        jot_diagSection("3. tmux session 'jot'")
        if not _tmux_session_exists('jot'):
        else:
          _tmux_run('list-sessions')
            try:
              tmux [blocking subproc]
            except FileNotFoundError: -> return '(tmux not found)'
          jot_diagIndent(jot_sessions + '\n')
            if not text:
          jot_diagIndent(_tmux_run('list-windows', '-t', 'jot') + '\n')
            if not text:
          _tmux_run('list-windows', '-t', 'jot')
            try:
              tmux [blocking subproc]
            except FileNotFoundError: -> return '(tmux not found)'
          jot_diagIndent(_tmux_run('list-panes', '-t', tmux_target, '-F', '#{pane_id} pid=#{pane_pid} dead=#{pane_dead} deadstatus=#{pane_dead_status} cmd=#{pane_current_command}') + '\n')
            if not text:
          _tmux_run('list-panes', '-t', tmux_target, '-F', '#{pane_id} pid=#{pane_pid} dead=#{pane_dead} deadstatus=#{pane_dead_status} cmd=#{pane_current_command}')
            try:
              tmux [blocking subproc]
            except FileNotFoundError: -> return '(tmux not found)'
          jot_diagIndent(_tmux_run('display-message', '-t', tmux_target, '-p', 'start: #{pane_start_command}') + '\n')
            if not text:
          _tmux_run('display-message', '-t', tmux_target, '-p', 'start: #{pane_start_command}')
            try:
              tmux [blocking subproc]
            except FileNotFoundError: -> return '(tmux not found)'
          _tmux_run('list-clients', '-t', 'jot')
            try:
              tmux [blocking subproc]
            except FileNotFoundError: -> return '(tmux not found)'
          if not clients.strip():
          else:
            jot_diagIndent(clients + '\n')
              if not text:
          _tmux_run('capture-pane', '-p', '-t', tmux_target, '-S', '-80')
            try:
              tmux [blocking subproc]
            except FileNotFoundError: -> return '(tmux not found)'
          jot_diagIndent(pane_content + '\n')
            if not text:
        jot_diagSection('4. /tmp/jot.* per-invocation dirs')
        for d in sorted(Path('/tmp').glob('jot.*')):
          if not d.is_dir():
          try:
            ls -la [blocking subproc]
          except FileNotFoundError:
          jot_diagIndent(ls_out)
            if not text:
          if settings.exists():
            jot_diagIndent(settings.read_text(errors='replace'))
              if not text:
        if not found_tmp:
        jot_diagSection(f'5. {log_file_path} (last 20 entries)')
        if log_path.exists():
          jot_diagIndent(_util_tail_lines(log_path, 20))
            if not text:
          _util_tail_lines(log_path, 20)
            try:
            except OSError: -> return ''
        else:
        jot_diagSection('6. Todos/ directory listing (newest first)')
        if todos_dir.is_dir():
          try:
            ls -lat [blocking subproc]
          except FileNotFoundError:
          jot_diagIndent(head_20)
            if not text:
        else:
        jot_diagSection('7. Installed plugin orchestrator path')
        for p_str in [os.path.join(plugin_root, 'scripts/jot_plugin_orchestrator.py'), os.path.join(plugin_root, 'scripts'), os.path.join(plugin_root, 'hooks/hooks.json')]:
          if p.is_symlink():
            jot_diagKv(p_str, f'-> {os.readlink(p_str)}')
          if p.exists():
            try:
            except OSError:
            jot_diagKv(p_str, f'present ({size} bytes)')
          else:
            jot_diagKv(p_str, 'MISSING')
        jot_diagSection('8. Dependency check')
        for cmd in ('jq', 'python3', 'tmux', 'claude', 'osascript'):
          jot_diagKv(cmd, which if which else 'NOT FOUND')
        jot_diagSection('END OF REPORT')
    if fn is not None:
  try:
  except json.JSONDecodeError:
  if prompt.startswith('/jot:'):
    if isinstance(data, dict):
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])): -> return 0
    jot_main()  # _PROMPT_DISPATCH['/jot']
      if not plugin_root or not plugin_data:
      if '"/jot' not in hook_input: -> return 0
      _util_append_log(log_file, f'{datetime.now().isoformat()} HOOK_INPUT {hook_input}\n')
        if not log_file:
        try:
        except OSError:
      hookjson_checkRequirements('jot', 'jq', 'python3', 'tmux', 'claude')
        for cmd in cmds:
          if shutil.which(cmd) is None:
            hookjson_installHint(cmd)
        if not missing: -> return None
        hookjson_emitBlock(f'{prefix} needs: {joined} - install and retry.')
      if tmux_requireVersion('2.9') != 0:
        hookjson_emitBlock('jot requires tmux 2.9+') -> return 0
      try:
      except json.JSONDecodeError:
      _util_strip_stdin_text(str(payload.get('prompt', '')))
      if prompt != '/jot' and (not prompt.startswith('/jot ')): -> return 0
      if idea.startswith(' '):
      _util_strip_stdin_text(idea)
      if not idea:
        hookjson_emitBlock('jot: no idea provided') -> return 0
      _util_append_log(log_file, f'{datetime.now().isoformat()} jot session={session_id} idea_len={len(idea)}\n')
        if not log_file:
        try:
        except OSError:
      try:
        git -C [blocking subproc]
      except (OSError, subprocess.SubprocessError):
      if not repo_root:
        hookjson_emitBlock("jot requires a git repository. Run 'git init' in your project root.") -> return 0
      _util_safe_call(git_getBranchNameOrFail, cwd)
        try:
          if result is None: -> return '(unavailable)'
        except Exception: -> return '(unavailable)'
      _util_safe_call(git_getRecentCommitHashes, cwd)
        try:
          if result is None: -> return '(unavailable)'
        except Exception: -> return '(unavailable)'
      _util_safe_call(git_getUncommittedFilenames, cwd)
        try:
          if result is None: -> return '(unavailable)'
        except Exception: -> return '(unavailable)'
      _util_safe_call(todo_scanOpen, repo_root)
        try:
          if result is None: -> return '(unavailable)'
        except Exception: -> return '(unavailable)'
      if transcript_path and Path(transcript_path).is_file():
        try:
          skills/jot/scripts/capture-conversation.py::main() [blocking subproc]
            (no resolvable entry function in target)
          if not conversation:
        except (OSError, subprocess.SubprocessError):
      else:
      try:
        common/scripts/jot/render_template.py::main() [blocking subproc]
          (no resolvable entry function in target)
      except (OSError, subprocess.SubprocessError):
      if not rewritten.endswith('\n'):
      if os.environ.get('JOT_SKIP_LAUNCH') == '1':
        hookjson_emitBlock(f'Jotted: {idea} (launch skipped)') -> return 0
      if log_file:
      jot_launchPhase2Window()
        jot_initState(state_dir)
          for name in ('queue.txt', 'active_job.txt', 'audit.log'):
        try:
        except LockTimeout:
          _jot_appendLog(log_file, f'[jot] failed to acquire global tmux-launch lock at {tmux_lock}\n') -> return 1
            if not log_file:
            try:
            except OSError:
        terminal_spawnIfNeeded('jot', log_file, 'jot') -> return 0
          if not session:
          _terminal_listTmuxClients(session)
            try:
              tmux list-clients [blocking subproc]
              if result.returncode != 0: -> return ''
            except (OSError, FileNotFoundError): -> return ''
          if clients.strip(): -> return 0
          if sys.platform == 'darwin':
            if shutil.which('osascript') is None:
              _terminal_appendAdvisory(log_file, log_prefix, session) -> return 0
                if not log_file or log_file == '/dev/null':
                try:
                except OSError:
            _terminal_buildOsascript(session, maximize)
            try:
              osascript [FaF subproc]
              try:
              finally:
            except (OSError, subprocess.SubprocessError): -> return 0
          _terminal_appendNonDarwinAdvisory(log_file, log_prefix, session) -> return 0
            if not log_file or log_file == '/dev/null':
            try:
            except OSError:
      hookjson_emitBlock(f'Done! Jotted idea in {input_abs}') -> return 0
    plate_main()  # _PROMPT_DISPATCH['/plate']
      if not plugin_root:
      if not plugin_data:
      if '/plate' not in raw_input: -> return 0
      try:
      except json.JSONDecodeError: -> return 0
      if not _PROMPT_RE_PLATE.match(prompt): -> return 0
      try:
      except GitError:
      if not repo_root_raw: -> return 0
      if not plate_log_override:
      try:
      except OSError:
      if prompt == '/plate':
      if prompt == '/plate --done':
      if prompt == '/plate --drop':
      if prompt == '/plate --trash':
      if prompt == '/plate --recycle':
      if prompt == '/plate --recycle --list':
      if prompt.startswith('/plate --recycle '):
      if prompt == '/plate --show':
      if prompt == '/plate --next':
      if prompt.startswith('/plate --next '):
      else: -> return 0
      try:
        common/scripts/plate/plate_cli.py::main() [blocking subproc]
          main()
            if argv is None:
            if not argv: -> return 0
            via _DISPATCH:
              _cmd_push()  # _DISPATCH['push']
                if len(argv) != 3: -> return 'plate: usage: push <convo_id> <transcript_path> <cwd>'
                plate_extractConvoNameFromTranscript(tp)
                  try:
                    if latest is not None:
                  except OSError: -> return None
                plate_push(repo)
                  git_getCurrentBranchName(repo)
                  _plate_resolveTargetPlate(repo, base_plate_name, convo_id)
                    if not git_checkIfBranchExists(repo, base_plate_name):
                  git_checkIfBranchExists(repo, base_plate_name)
                    git show-ref [blocking subproc]
                  git_getCommitTrailers(repo, base_plate_name)
                    run(['git', 'log', '-1', '--format=%(trailers:only,unfold=true)', ref])
                      if env is not None:
                      [blocking subproc] <unresolved argv>
                      if not capture: -> return ''
                    for line in raw.splitlines():
                      if ':' in line:
                  if use_extraction:
                    _plate_buildExtractedTree(repo, base_plate_name, convo_id, parent)
                      plate_findMyLastPlate(repo, plate_branch, convo_id)
                        if not git_checkIfBranchExists(repo, plate_branch):
                        run(['git', 'log', plate_branch, '--format=%H|%ci|%(trailers:key=convo-id,valueonly,unfold=true)'])
                          if env is not None:
                          [blocking subproc] <unresolved argv>
                          if not capture: -> return ''
                        for line in raw.splitlines():
                          if len(parts) != 3:
                          if trailer.strip() == convo_id:
                      plate_extractFilesEditedSinceTimestamp(transcript_arg)
                        try:
                        except OSError:
                      plate_extractFilesCreatedSinceTimestamp(transcript_arg)
                        for cmd in _plate_iterBashCommandsSinceTimestamp(transcript_path, since_iso):
                          _plate_parseFilesCreatedFromBashCommand(cmd, repo_root_resolved)
                            try:
                            except ValueError:
                            while i < n:
                              if tok in ('&&', '||', ';', '|'):
                                if cmd_mode == 'cp_mv':
                                  _flushCpMvDestination()
                                    if pending_positionals:
                                      _addIfInsideRepo(pending_positionals[-1])
                                        if any((c in tok for c in _SHELL_EXPANSION_CHARS)):
                                        if tok.startswith('-'):
                                        try:
                                        except (OSError, ValueError):
                              if tok in ('>', '>>'):
                              if pending_redirect:
                                _addIfInsideRepo(tok)
                                  if any((c in tok for c in _SHELL_EXPANSION_CHARS)):
                                  if tok.startswith('-'):
                                  try:
                                  except (OSError, ValueError):
                              if cmd_mode is None:
                                if tok == 'git' and i + 1 < n and (tokens[i + 1] == 'add'):
                                if tok == 'touch':
                                if tok == 'tee':
                                if tok in ('cp', 'mv'):
                              if tok.startswith('-'):
                              if any((c in tok for c in _SHELL_EXPANSION_CHARS)):
                              if cmd_mode in ('touch', 'tee', 'git_add'):
                                _addIfInsideRepo(tok)
                                  if any((c in tok for c in _SHELL_EXPANSION_CHARS)):
                                  if tok.startswith('-'):
                                  try:
                                  except (OSError, ValueError):
                              if cmd_mode == 'cp_mv':
                            if cmd_mode == 'cp_mv':
                              _flushCpMvDestination()
                                if pending_positionals:
                                  _addIfInsideRepo(pending_positionals[-1])
                                    if any((c in tok for c in _SHELL_EXPANSION_CHARS)):
                                    if tok.startswith('-'):
                                    try:
                                    except (OSError, ValueError):
                      for rel in bash_created_rel:
                      plate_extractFilesDeletedSinceTimestamp(transcript_arg)
                        try:
                        except OSError:
                      run(['git', 'ls-tree', '-r', '--name-only', parent_sha])
                        if env is not None:
                        [blocking subproc] <unresolved argv>
                        if not capture: -> return ''
                      for abs_path in edited_abs:
                        try:
                        except (OSError, ValueError):
                      git_makeTempIndexPath()
                      try:
                        git_setIndexFileForEnv()
                        git_readTreeAt()
                        for rel_path in edited_relative:
                          if (repo / rel_path).exists():
                            run(['git', 'add', '--', rel_path])
                              if env is not None:
                              [blocking subproc] <unresolved argv>
                              if not capture: -> return ''
                        for rel_path in deleted:
                          run(['git', 'rm', '--cached', '--ignore-unmatch', '--', rel_path])
                            if env is not None:
                            [blocking subproc] <unresolved argv>
                            if not capture: -> return ''
                      finally:
                  else:
                    _plate_buildFullWtTree(repo)
                      git_makeTempIndexPath()
                      try:
                        git_setIndexFileForEnv()
                        git_readTreeAt()
                        git_stageAllChanges()
                          run(['git', 'add', '-A'])
                            if env is not None:
                            [blocking subproc] <unresolved argv>
                            if not capture: -> return ''
                      finally:
                  git_getSHAForRefViaRevParse()
                  git_getTreeRevOf(parent)
                  if commit_tree == parent_tree:
                    if use_extraction: -> return None
                      _plate_buildFullWtTree(repo)
                        git_makeTempIndexPath()
                        try:
                          git_setIndexFileForEnv()
                          git_readTreeAt()
                          git_stageAllChanges()
                            run(['git', 'add', '-A'])
                              if env is not None:
                              [blocking subproc] <unresolved argv>
                              if not capture: -> return ''
                        finally:
                      if full_wt_tree != parent_tree:
                        _plate_logExtractEmptyButWtDirty()
                          _plate_resolvePlateLogFile(repo)
                            if env_path:
                            else:
                  if convo_id is not None:
                  if convo_name is not None:
                  if convo_summary is not None:
                    _plate_formatTrailerBody(convo_summary)
                      if not raw: -> return ''
                  run(['git', 'commit-tree', wt_tree, '-p', parent, COMMIT_MESSAGE_FLAG, commitMessage])
                    if env is not None:
                    [blocking subproc] <unresolved argv>
                    if not capture: -> return ''
                  run(['git', 'update-ref', f'refs/heads/{target_plate}', new_commit])
                    if env is not None:
                    [blocking subproc] <unresolved argv>
                    if not capture: -> return ''
                if sha is None: -> return 'plate: no changes to stack'
                try:
                  _spawn_summary(repo, branch, sha, transcript_path)
                    if os.environ.get('PLATE_SKIP_LAUNCH') == '1': -> return None
                    if not shutil.which('tmux') or not shutil.which('claude'): -> return None
                    _abs(plate_skill_dir)
                    _abs(output_dir)
                    _abs(output_dir)
                    _abs(output_dir)
                    if transcript_path:
                      _abs(project_dir)
                    bgPermissions_loadClaude('plate')
                      _loadSection(tool, 'claude', bundle_path, log_file)
                        _seedIfNeeded(bundle_path)
                          _resolveInstalled()
                            if not plugin_data:
                          _resolveBundle(bundle_path)
                            if bundle_path is not None:
                            if not plugin_root:
                          claude_seedPermissions(str(installed_path), str(bundle_json), str(bundle_sha), str(prior_sha_path), log_file, 'bg_agent_permissions')
                            if not Path(default).is_file() or not Path(default_sha_file).is_file():
                              claude_permseedLog(f'bundled permissions default missing at {default} - cannot seed', log_file, log_prefix)
                                if not log_file:
                                try:
                                except OSError:
                            _util_readFirstToken(default_sha_file)
                            if not Path(installed).is_file():
                              claude_permseedLog(f'seeded {installed} from bundled default (sha={current_default_sha})', log_file, log_prefix)
                                if not log_file:
                                try:
                                except OSError:
                            try:
                              _util_sha256File(installed)
                            except OSError:
                            _util_readFirstToken(prior_sha_file)
                            if installed_sha == current_default_sha:
                            if prior_sha and installed_sha == prior_sha:
                              claude_permseedLog(f'upgraded {installed} to new bundled default (was {prior_sha}, now {current_default_sha})', log_file, log_prefix)
                                if not log_file:
                                try:
                                except OSError:
                            if prior_sha != current_default_sha:
                              claude_permseedLog(f'{installed} is user-edited; bundled default updated - diff manually. installed_sha={installed_sha} prior_sha={prior_sha} current_default_sha={current_default_sha}', log_file, log_prefix)
                                if not log_file:
                                try:
                                except OSError:
                        try:
                        except (OSError, json.JSONDecodeError):
                          _resolveBundle(bundle_path)
                            if bundle_path is not None:
                            if not plugin_root:
                        if key not in data:
                        if worker not in section:
                      _substituteTemplates(section.get('allow', []), env)
                        for item in items:
                      if extra_allow:
                    _next_session_index()
                      try:
                      except (FileNotFoundError, ValueError):
                    if log_file:
                    tmux new-session [FaF subproc]
                    scripts/jot_plugin_orchestrator.py::main() [FaF subproc]
                      plate_summaryWatch() [via orchestrator argv 'plate-summary-watch']
                        if timeout is None:
                        if interval is None:
                        while elapsed < timeout: -> return 1
                          if _ready():
                            try:
                            except Exception:
                            try:
                            except Exception: -> return 0
                    try:
                      terminal_spawnIfNeeded(session_name, terminal_log_arg, 'plate', 'compact')
                        if not session:
                        _terminal_listTmuxClients(session)
                          try:
                            tmux list-clients [blocking subproc]
                            if result.returncode != 0: -> return ''
                          except (OSError, FileNotFoundError): -> return ''
                        if clients.strip(): -> return 0
                        if sys.platform == 'darwin':
                          if shutil.which('osascript') is None:
                            _terminal_appendAdvisory(log_file, log_prefix, session) -> return 0
                              if not log_file or log_file == '/dev/null':
                              try:
                              except OSError:
                          _terminal_buildOsascript(session, maximize)
                          try:
                            osascript [FaF subproc]
                            try:
                            finally:
                          except (OSError, subprocess.SubprocessError): -> return 0
                        _terminal_appendNonDarwinAdvisory(log_file, log_prefix, session) -> return 0
                          if not log_file or log_file == '/dev/null':
                          try:
                          except OSError:
                    except Exception:
                except Exception:
                if attach_hint:
              _cmd_done()  # _DISPATCH['done']
                if len(argv) != 1: -> return 'plate: usage: done <repo>'
                plate_done(Path(argv[0])) -> return 'plate: replayed plate stack onto current branch'
                  if branch is None:
                    git_getCurrentBranchName(repo)
                  if not git_checkIfBranchExists(repo, plateBranchName):
                  _plate_buildFullWtTree(repo)
                    git_makeTempIndexPath()
                    try:
                      git_setIndexFileForEnv()
                      git_readTreeAt()
                      git_stageAllChanges()
                        run(['git', 'add', '-A'])
                          if env is not None:
                          [blocking subproc] <unresolved argv>
                          if not capture: -> return ''
                    finally:
                  git_getTreeSHA(repo, plateBranchName)
                  if wt_tree != plate_tip_tree:
                  git_getSHAForRefViaRevParse(repo, 'HEAD')
                  git_resetHardToHead(repo)
                    run(['git', 'reset', QUIET_OUTPUT, '--hard'])
                      if env is not None:
                      [blocking subproc] <unresolved argv>
                      if not capture: -> return ''
                  git_cleanWorkTree(repo)
                    run(['git', 'clean', '-fd', QUIET_OUTPUT])
                      if env is not None:
                      [blocking subproc] <unresolved argv>
                      if not capture: -> return ''
                  git cherry-pick [blocking subproc]
                  if completed.returncode != 0:
                    git cherry-pick [blocking subproc]
                    run(['git', 'reset', QUIET_OUTPUT, '--hard', preHeadSha])
                      if env is not None:
                      [blocking subproc] <unresolved argv>
                      if not capture: -> return ''
                    git_cleanWorkTree(repo)
                      run(['git', 'clean', '-fd', QUIET_OUTPUT])
                        if env is not None:
                        [blocking subproc] <unresolved argv>
                        if not capture: -> return ''
                  git_deleteBranchByForce(repo, plateBranchName)
                    run(['git', 'branch', '-D', QUIET_OUTPUT, branchName])
                      if env is not None:
                      [blocking subproc] <unresolved argv>
                      if not capture: -> return ''
              _cmd_drop()  # _DISPATCH['drop']
                if len(argv) != 1: -> return 'plate: usage: drop <repo>'
                plate_drop(Path(argv[0]))
                  if branch is None:
                    git_getCurrentBranchName(repo)
                  if not git_checkIfBranchExists(repo, plateBranchName): -> return None
                  git_getSHAForRefViaRevParse(repo, plateBranchName)
                  run(['git', 'rev-parse', f'{plateBranchName}~1'])
                    if env is not None:
                    [blocking subproc] <unresolved argv>
                    if not capture: -> return ''
                  run(['git', 'diff', '--binary', parent_sha, tip_sha])
                    if env is not None:
                    [blocking subproc] <unresolved argv>
                    if not capture: -> return ''
                  _plate_writeTrashSession()
                    git_test_currentTimestampUtcCompact()
                    _plate_trashBranchDir(repo, branch)
                    for (filename, patch_text) in patches:
                  git_getCommitTrailers(repo, tip_sha)
                    run(['git', 'log', '-1', '--format=%(trailers:only,unfold=true)', ref])
                      if env is not None:
                      [blocking subproc] <unresolved argv>
                      if not capture: -> return ''
                    for line in raw.splitlines():
                      if ':' in line:
                  run(['git', 'rev-list', '--count', f'{branch}..{plateBranchName}'])
                    if env is not None:
                    [blocking subproc] <unresolved argv>
                    if not capture: -> return ''
                  if plateCount == 1:
                    git_deleteBranchByForce(repo, plateBranchName)
                      run(['git', 'branch', '-D', QUIET_OUTPUT, branchName])
                        if env is not None:
                        [blocking subproc] <unresolved argv>
                        if not capture: -> return ''
                  else:
                    run(['git', 'update-ref', f'refs/heads/{plateBranchName}', parent_sha])
                      if env is not None:
                      [blocking subproc] <unresolved argv>
                      if not capture: -> return ''
                if result is None: -> return 'plate: no plate to drop'
              _cmd_trash()  # _DISPATCH['trash']
                if len(argv) < 1 or len(argv) > 2: -> return 'plate: usage: trash <repo> [--clean-wt]'
                plate_trash(repo)
                  if branch is None:
                    git_getCurrentBranchName(repo)
                  if not git_checkIfBranchExists(repo, plateBranchName): -> return None
                  run(['git', 'rev-list', '--reverse', f'{branch}..{plateBranchName}'])
                    if env is not None:
                    [blocking subproc] <unresolved argv>
                    if not capture: -> return ''
                  run(['git', 'rev-parse', f'{plates[0]}~1'])
                    if env is not None:
                    [blocking subproc] <unresolved argv>
                    if not capture: -> return ''
                  for (i, plate_sha) in enumerate(plates, start=1):
                    run(['git', 'diff', '--binary', f'{plate_sha}~1', plate_sha])
                      if env is not None:
                      [blocking subproc] <unresolved argv>
                      if not capture: -> return ''
                    git_getCommitTrailers(repo, plate_sha)
                      run(['git', 'log', '-1', '--format=%(trailers:only,unfold=true)', ref])
                        if env is not None:
                        [blocking subproc] <unresolved argv>
                        if not capture: -> return ''
                      for line in raw.splitlines():
                        if ':' in line:
                  _plate_writeTrashSession()
                    git_test_currentTimestampUtcCompact()
                    _plate_trashBranchDir(repo, branch)
                    for (filename, patch_text) in patches:
                  git_deleteBranchByForce(repo, plateBranchName)
                    run(['git', 'branch', '-D', QUIET_OUTPUT, branchName])
                      if env is not None:
                      [blocking subproc] <unresolved argv>
                      if not capture: -> return ''
                  if clean_wt:
                    git_resetHardToHead(repo)
                      run(['git', 'reset', QUIET_OUTPUT, '--hard'])
                        if env is not None:
                        [blocking subproc] <unresolved argv>
                        if not capture: -> return ''
                    git_cleanWorkTree(repo)
                      run(['git', 'clean', '-fd', QUIET_OUTPUT])
                        if env is not None:
                        [blocking subproc] <unresolved argv>
                        if not capture: -> return ''
                if result is None: -> return 'plate: no plate to trash'
              _cmd_recycle()  # _DISPATCH['recycle']
                if len(argv) < 1 or len(argv) > 2: -> return 'plate: usage: recycle <repo> [--list|<session-dir-name>]'
                if len(argv) == 2:
                  if argv[1] == '--list':
                  plate_recycle(repo)
                    if branch is None:
                      git_getCurrentBranchName(repo)
                    _plate_listTrashSessions(repo, branch)
                      _plate_trashBranchDir(repo, branch)
                      if not branch_dir.is_dir():
                    if not sessions: -> return None
                    if session is not None:
                      if chosen is None: -> return None
                    if timestamp is not None:
                      if chosen is None: -> return None
                    else:
                    try:
                    except (OSError, ValueError): -> return None
                    if not parent_sha: -> return None
                    try:
                      run(['git', 'cat-file', '-e', parent_sha])
                        if env is not None:
                        [blocking subproc] <unresolved argv>
                        if not capture: -> return ''
                    except Exception: -> return None
                    if git_checkIfBranchExists(repo, plateBranchName):
                      git_deleteBranchByForce(repo, plateBranchName)
                        run(['git', 'branch', '-D', QUIET_OUTPUT, branchName])
                          if env is not None:
                          [blocking subproc] <unresolved argv>
                          if not capture: -> return ''
                    run(['git', 'update-ref', f'refs/heads/{plateBranchName}', parent_sha])
                      if env is not None:
                      [blocking subproc] <unresolved argv>
                      if not capture: -> return ''
                    for patch in patches:
                      git_applyPatch(repo, patch)
                        run(['git', 'apply', '--3way', str(patch)])
                          if env is not None:
                          [blocking subproc] <unresolved argv>
                          if not capture: -> return ''
                      plate_push(repo)
                        git_getCurrentBranchName(repo)
                        _plate_resolveTargetPlate(repo, base_plate_name, convo_id)
                          if not git_checkIfBranchExists(repo, base_plate_name):
                        git_checkIfBranchExists(repo, base_plate_name)
                          git show-ref [blocking subproc]
                        git_getCommitTrailers(repo, base_plate_name)
                          run(['git', 'log', '-1', '--format=%(trailers:only,unfold=true)', ref])
                            if env is not None:
                            [blocking subproc] <unresolved argv>
                            if not capture: -> return ''
                          for line in raw.splitlines():
                            if ':' in line:
                        if use_extraction:
                          _plate_buildExtractedTree(repo, base_plate_name, convo_id, parent)
                            plate_findMyLastPlate(repo, plate_branch, convo_id)
                              if not git_checkIfBranchExists(repo, plate_branch):
                              run(['git', 'log', plate_branch, '--format=%H|%ci|%(trailers:key=convo-id,valueonly,unfold=true)'])
                                if env is not None:
                                [blocking subproc] <unresolved argv>
                                if not capture: -> return ''
                              for line in raw.splitlines():
                                if len(parts) != 3:
                                if trailer.strip() == convo_id:
                            plate_extractFilesEditedSinceTimestamp(transcript_arg)
                              try:
                              except OSError:
                            plate_extractFilesCreatedSinceTimestamp(transcript_arg)
                              for cmd in _plate_iterBashCommandsSinceTimestamp(transcript_path, since_iso):
                                _plate_parseFilesCreatedFromBashCommand(cmd, repo_root_resolved)
                                  try:
                                  except ValueError:
                                  while i < n:
                                    if tok in ('&&', '||', ';', '|'):
                                      if cmd_mode == 'cp_mv':
                                        _flushCpMvDestination()
                                          if pending_positionals:
                                            _addIfInsideRepo(pending_positionals[-1])
                                              if any((c in tok for c in _SHELL_EXPANSION_CHARS)):
                                              if tok.startswith('-'):
                                              try:
                                              except (OSError, ValueError):
                                    if tok in ('>', '>>'):
                                    if pending_redirect:
                                      _addIfInsideRepo(tok)
                                        if any((c in tok for c in _SHELL_EXPANSION_CHARS)):
                                        if tok.startswith('-'):
                                        try:
                                        except (OSError, ValueError):
                                    if cmd_mode is None:
                                      if tok == 'git' and i + 1 < n and (tokens[i + 1] == 'add'):
                                      if tok == 'touch':
                                      if tok == 'tee':
                                      if tok in ('cp', 'mv'):
                                    if tok.startswith('-'):
                                    if any((c in tok for c in _SHELL_EXPANSION_CHARS)):
                                    if cmd_mode in ('touch', 'tee', 'git_add'):
                                      _addIfInsideRepo(tok)
                                        if any((c in tok for c in _SHELL_EXPANSION_CHARS)):
                                        if tok.startswith('-'):
                                        try:
                                        except (OSError, ValueError):
                                    if cmd_mode == 'cp_mv':
                                  if cmd_mode == 'cp_mv':
                                    _flushCpMvDestination()
                                      if pending_positionals:
                                        _addIfInsideRepo(pending_positionals[-1])
                                          if any((c in tok for c in _SHELL_EXPANSION_CHARS)):
                                          if tok.startswith('-'):
                                          try:
                                          except (OSError, ValueError):
                            for rel in bash_created_rel:
                            plate_extractFilesDeletedSinceTimestamp(transcript_arg)
                              try:
                              except OSError:
                            run(['git', 'ls-tree', '-r', '--name-only', parent_sha])
                              if env is not None:
                              [blocking subproc] <unresolved argv>
                              if not capture: -> return ''
                            for abs_path in edited_abs:
                              try:
                              except (OSError, ValueError):
                            git_makeTempIndexPath()
                            try:
                              git_setIndexFileForEnv()
                              git_readTreeAt()
                              for rel_path in edited_relative:
                                if (repo / rel_path).exists():
                                  run(['git', 'add', '--', rel_path])
                                    if env is not None:
                                    [blocking subproc] <unresolved argv>
                                    if not capture: -> return ''
                              for rel_path in deleted:
                                run(['git', 'rm', '--cached', '--ignore-unmatch', '--', rel_path])
                                  if env is not None:
                                  [blocking subproc] <unresolved argv>
                                  if not capture: -> return ''
                            finally:
                        else:
                          _plate_buildFullWtTree(repo)
                            git_makeTempIndexPath()
                            try:
                              git_setIndexFileForEnv()
                              git_readTreeAt()
                              git_stageAllChanges()
                                run(['git', 'add', '-A'])
                                  if env is not None:
                                  [blocking subproc] <unresolved argv>
                                  if not capture: -> return ''
                            finally:
                        git_getSHAForRefViaRevParse()
                        git_getTreeRevOf(parent)
                        if commit_tree == parent_tree:
                          if use_extraction: -> return None
                            _plate_buildFullWtTree(repo)
                              git_makeTempIndexPath()
                              try:
                                git_setIndexFileForEnv()
                                git_readTreeAt()
                                git_stageAllChanges()
                                  run(['git', 'add', '-A'])
                                    if env is not None:
                                    [blocking subproc] <unresolved argv>
                                    if not capture: -> return ''
                              finally:
                            if full_wt_tree != parent_tree:
                              _plate_logExtractEmptyButWtDirty()
                                _plate_resolvePlateLogFile(repo)
                                  if env_path:
                                  else:
                        if convo_id is not None:
                        if convo_name is not None:
                        if convo_summary is not None:
                          _plate_formatTrailerBody(convo_summary)
                            if not raw: -> return ''
                        run(['git', 'commit-tree', wt_tree, '-p', parent, COMMIT_MESSAGE_FLAG, commitMessage])
                          if env is not None:
                          [blocking subproc] <unresolved argv>
                          if not capture: -> return ''
                        run(['git', 'update-ref', f'refs/heads/{target_plate}', new_commit])
                          if env is not None:
                          [blocking subproc] <unresolved argv>
                          if not capture: -> return ''
                else:
                  plate_recycle(repo)
                    if branch is None:
                      git_getCurrentBranchName(repo)
                    _plate_listTrashSessions(repo, branch)
                      _plate_trashBranchDir(repo, branch)
                      if not branch_dir.is_dir():
                    if not sessions: -> return None
                    if session is not None:
                      if chosen is None: -> return None
                    if timestamp is not None:
                      if chosen is None: -> return None
                    else:
                    try:
                    except (OSError, ValueError): -> return None
                    if not parent_sha: -> return None
                    try:
                      run(['git', 'cat-file', '-e', parent_sha])
                        if env is not None:
                        [blocking subproc] <unresolved argv>
                        if not capture: -> return ''
                    except Exception: -> return None
                    if git_checkIfBranchExists(repo, plateBranchName):
                      git_deleteBranchByForce(repo, plateBranchName)
                        run(['git', 'branch', '-D', QUIET_OUTPUT, branchName])
                          if env is not None:
                          [blocking subproc] <unresolved argv>
                          if not capture: -> return ''
                    run(['git', 'update-ref', f'refs/heads/{plateBranchName}', parent_sha])
                      if env is not None:
                      [blocking subproc] <unresolved argv>
                      if not capture: -> return ''
                    for patch in patches:
                      git_applyPatch(repo, patch)
                        run(['git', 'apply', '--3way', str(patch)])
                          if env is not None:
                          [blocking subproc] <unresolved argv>
                          if not capture: -> return ''
                      plate_push(repo)
                        git_getCurrentBranchName(repo)
                        _plate_resolveTargetPlate(repo, base_plate_name, convo_id)
                          if not git_checkIfBranchExists(repo, base_plate_name):
                        git_checkIfBranchExists(repo, base_plate_name)
                          git show-ref [blocking subproc]
                        git_getCommitTrailers(repo, base_plate_name)
                          run(['git', 'log', '-1', '--format=%(trailers:only,unfold=true)', ref])
                            if env is not None:
                            [blocking subproc] <unresolved argv>
                            if not capture: -> return ''
                          for line in raw.splitlines():
                            if ':' in line:
                        if use_extraction:
                          _plate_buildExtractedTree(repo, base_plate_name, convo_id, parent)
                            plate_findMyLastPlate(repo, plate_branch, convo_id)
                              if not git_checkIfBranchExists(repo, plate_branch):
                              run(['git', 'log', plate_branch, '--format=%H|%ci|%(trailers:key=convo-id,valueonly,unfold=true)'])
                                if env is not None:
                                [blocking subproc] <unresolved argv>
                                if not capture: -> return ''
                              for line in raw.splitlines():
                                if len(parts) != 3:
                                if trailer.strip() == convo_id:
                            plate_extractFilesEditedSinceTimestamp(transcript_arg)
                              try:
                              except OSError:
                            plate_extractFilesCreatedSinceTimestamp(transcript_arg)
                              for cmd in _plate_iterBashCommandsSinceTimestamp(transcript_path, since_iso):
                                _plate_parseFilesCreatedFromBashCommand(cmd, repo_root_resolved)
                                  try:
                                  except ValueError:
                                  while i < n:
                                    if tok in ('&&', '||', ';', '|'):
                                      if cmd_mode == 'cp_mv':
                                        _flushCpMvDestination()
                                          if pending_positionals:
                                            _addIfInsideRepo(pending_positionals[-1])
                                              if any((c in tok for c in _SHELL_EXPANSION_CHARS)):
                                              if tok.startswith('-'):
                                              try:
                                              except (OSError, ValueError):
                                    if tok in ('>', '>>'):
                                    if pending_redirect:
                                      _addIfInsideRepo(tok)
                                        if any((c in tok for c in _SHELL_EXPANSION_CHARS)):
                                        if tok.startswith('-'):
                                        try:
                                        except (OSError, ValueError):
                                    if cmd_mode is None:
                                      if tok == 'git' and i + 1 < n and (tokens[i + 1] == 'add'):
                                      if tok == 'touch':
                                      if tok == 'tee':
                                      if tok in ('cp', 'mv'):
                                    if tok.startswith('-'):
                                    if any((c in tok for c in _SHELL_EXPANSION_CHARS)):
                                    if cmd_mode in ('touch', 'tee', 'git_add'):
                                      _addIfInsideRepo(tok)
                                        if any((c in tok for c in _SHELL_EXPANSION_CHARS)):
                                        if tok.startswith('-'):
                                        try:
                                        except (OSError, ValueError):
                                    if cmd_mode == 'cp_mv':
                                  if cmd_mode == 'cp_mv':
                                    _flushCpMvDestination()
                                      if pending_positionals:
                                        _addIfInsideRepo(pending_positionals[-1])
                                          if any((c in tok for c in _SHELL_EXPANSION_CHARS)):
                                          if tok.startswith('-'):
                                          try:
                                          except (OSError, ValueError):
                            for rel in bash_created_rel:
                            plate_extractFilesDeletedSinceTimestamp(transcript_arg)
                              try:
                              except OSError:
                            run(['git', 'ls-tree', '-r', '--name-only', parent_sha])
                              if env is not None:
                              [blocking subproc] <unresolved argv>
                              if not capture: -> return ''
                            for abs_path in edited_abs:
                              try:
                              except (OSError, ValueError):
                            git_makeTempIndexPath()
                            try:
                              git_setIndexFileForEnv()
                              git_readTreeAt()
                              for rel_path in edited_relative:
                                if (repo / rel_path).exists():
                                  run(['git', 'add', '--', rel_path])
                                    if env is not None:
                                    [blocking subproc] <unresolved argv>
                                    if not capture: -> return ''
                              for rel_path in deleted:
                                run(['git', 'rm', '--cached', '--ignore-unmatch', '--', rel_path])
                                  if env is not None:
                                  [blocking subproc] <unresolved argv>
                                  if not capture: -> return ''
                            finally:
                        else:
                          _plate_buildFullWtTree(repo)
                            git_makeTempIndexPath()
                            try:
                              git_setIndexFileForEnv()
                              git_readTreeAt()
                              git_stageAllChanges()
                                run(['git', 'add', '-A'])
                                  if env is not None:
                                  [blocking subproc] <unresolved argv>
                                  if not capture: -> return ''
                            finally:
                        git_getSHAForRefViaRevParse()
                        git_getTreeRevOf(parent)
                        if commit_tree == parent_tree:
                          if use_extraction: -> return None
                            _plate_buildFullWtTree(repo)
                              git_makeTempIndexPath()
                              try:
                                git_setIndexFileForEnv()
                                git_readTreeAt()
                                git_stageAllChanges()
                                  run(['git', 'add', '-A'])
                                    if env is not None:
                                    [blocking subproc] <unresolved argv>
                                    if not capture: -> return ''
                              finally:
                            if full_wt_tree != parent_tree:
                              _plate_logExtractEmptyButWtDirty()
                                _plate_resolvePlateLogFile(repo)
                                  if env_path:
                                  else:
                        if convo_id is not None:
                        if convo_name is not None:
                        if convo_summary is not None:
                          _plate_formatTrailerBody(convo_summary)
                            if not raw: -> return ''
                        run(['git', 'commit-tree', wt_tree, '-p', parent, COMMIT_MESSAGE_FLAG, commitMessage])
                          if env is not None:
                          [blocking subproc] <unresolved argv>
                          if not capture: -> return ''
                        run(['git', 'update-ref', f'refs/heads/{target_plate}', new_commit])
                          if env is not None:
                          [blocking subproc] <unresolved argv>
                          if not capture: -> return ''
                if result is None: -> return 'plate: nothing to recycle'
              _cmd_next()  # _DISPATCH['next']
                if len(argv) == 1:
                if len(argv) == 2: -> return 'plate: usage: next <repo> [<index>]'
              _cmd_show()  # _DISPATCH['show']
              _cmd_set_plate_summary()  # _DISPATCH['set-plate-summary']
                if len(argv) != 3: -> return 'plate: usage: set-plate-summary <repo> <branch> <summary-file>'
                plate_regenerateTipSummary(repo, branch)
                  _parseAgentPayload(new_payload)
                    if not text.strip():
                    if blank_idx is None:
                  if not body:
                  run(['git', 'rev-parse', plate_branch])
                    if env is not None:
                    [blocking subproc] <unresolved argv>
                    if not capture: -> return ''
                  run(['git', 'log', '-1', '--format=%B', tip_sha])
                    if env is not None:
                    [blocking subproc] <unresolved argv>
                    if not capture: -> return ''
                  run(['git', 'rev-parse', f'{tip_sha}^{{tree}}'])
                    if env is not None:
                    [blocking subproc] <unresolved argv>
                    if not capture: -> return ''
                  run(['git', 'rev-parse', f'{tip_sha}~1'])
                    if env is not None:
                    [blocking subproc] <unresolved argv>
                    if not capture: -> return ''
                  _stripSummaryTrailerFromMessage(tip_msg)
                  if subject:
                    _replaceCommitSubject(cleaned, subject)
                      if len(new_subject) > 50:
                      if not new_subject:
                      for (i, line) in enumerate(lines):
                        if stripped and (not stripped.startswith('#')):
                      if message.endswith('\n') and (not rebuilt.endswith('\n')):
                  _appendSummaryTrailerToMessage(cleaned, trailer_body)
                    while content and content[-1].strip() == '':
                    _format_trailer_body(summary)
                      if not raw: -> return ''
                    if comments:
                    if not rebuilt.endswith('\n'):
                  run(['git', 'commit-tree', tip_tree, '-p', tip_parent, COMMIT_MESSAGE_FLAG, new_msg])
                    if env is not None:
                    [blocking subproc] <unresolved argv>
                    if not capture: -> return ''
                  run(['git', 'update-ref', f'refs/heads/{plate_branch}', new_tip_sha])
                    if env is not None:
                    [blocking subproc] <unresolved argv>
                    if not capture: -> return ''
            if handler is None: -> return 0
      except Exception: -> return 0
        try:
        except OSError:
    debate_launch()  # _PROMPT_DISPATCH['/debate']
      if scripts_dir is None:
      if plugin_root is None:
      if is_darwin and (not skip_terminal_check):
        if not terminal_running_fn():
    debate_retryMain()  # _PROMPT_DISPATCH['/debate-retry']
      debate_initHookContext()
        if not plugin_root:
        if not plugin_data:
        try:
        except OSError:
        try:
          if not isinstance(payload, dict):
        except json.JSONDecodeError:
        try:
          git -C [blocking subproc]
        except (OSError, FileNotFoundError):
      hookjson_checkRequirements('debate-retry', 'jq', 'python3', 'tmux', 'claude')
        for cmd in cmds:
          if shutil.which(cmd) is None:
            hookjson_installHint(cmd)
        if not missing: -> return None
        hookjson_emitBlock(f'{prefix} needs: {joined} - install and retry.')
      if not transcript_path:
        hookjson_emitBlock('/debate-retry: no transcript_path in hook payload') -> return 0
      if not repo_root:
        hookjson_emitBlock('/debate-retry requires a git repository') -> return 0
      if debates_root.is_dir():
        for entry in debates_root.iterdir():
          if not entry.is_dir():
          if not marker.is_file():
          try:
          except OSError:
          if content != transcript_path and content.rstrip('\n') != transcript_path:
          if ts > best_ts:
      if best is None:
        hookjson_emitBlock('/debate-retry: no debate found in this conversation') -> return 0
      if (best / 'synthesis.md').exists():
        hookjson_emitBlock(f'/debate-retry: already complete, see {best}/synthesis.md') -> return 0
      if debate_anyLiveLock(str(best)):
        try:
          debate_liveSession(str(best))
            for lock_path in lock_files: -> return ''
              try:
              except OSError:
              if not match:
              try:
                tmux display-message [blocking subproc]
              except OSError:
              if proc.returncode != 0:
              if session:
        except Exception:
        hookjson_emitBlock(f'/debate-retry: still running -> tmux attach -t {live}')
      try:
      except OSError:
      debate_detectAvailableAgents()
        if gemini_out:
          if gemini_out != _PRESENT_SENTINEL:
        if codex_out:
          if codex_out != _PRESENT_SENTINEL:
      debate_checkResumeFeasibility(debate_dir, available_agents)
        if debate_dir.is_dir():
          for path in sorted(debate_dir.glob('r1_instructions_*.txt')):
            if not path.is_file():
            if agent:
        for orig in original:
          if orig in updated:
          if r1_ok and r2_ok:
          else:
        if unusable:
      try:
      except FileNotFoundError:
      except OSError:
      debate_startOrResume() -> return 0
        if resuming:
          for f in debate_dir.glob('r1_instructions_*.txt'):
          if original_agents != set(available_agents):
        for agent in available_agents:
          if not r1_path.exists():
            debate_buildClaudePrompts()
              if not agents:
              if stage == 'r1':
                _debate_build_r1(stage, debate_dir, plugin_root, agents, agent_filter)
                  for agent in agents:
                    if agent_filter and agent_filter != agent:
                    if render.exists():
                      common/scripts/jot/render_template.py::main() [blocking subproc]
                        (no resolvable entry function in target)
                    else:
              if stage == 'r2':
                _debate_build_r2(debate_dir, agents, agent_filter)
                  for agent in agents:
                    if agent_filter and agent_filter != agent:
                    for other in others:
              if stage == 'synthesis':
                _debate_build_synthesis(debate_dir, agents)
                  for agent in agents:
                  for agent in agents:
              else:
        for agent in available_agents:
          if not r2_path.exists():
            debate_buildClaudePrompts()
              if not agents:
              if stage == 'r1':
                _debate_build_r1(stage, debate_dir, plugin_root, agents, agent_filter)
                  for agent in agents:
                    if agent_filter and agent_filter != agent:
                    if render.exists():
                      common/scripts/jot/render_template.py::main() [blocking subproc]
                        (no resolvable entry function in target)
                    else:
              if stage == 'r2':
                _debate_build_r2(debate_dir, agents, agent_filter)
                  for agent in agents:
                    if agent_filter and agent_filter != agent:
                    for other in others:
              if stage == 'synthesis':
                _debate_build_synthesis(debate_dir, agents)
                  for agent in agents:
                  for agent in agents:
              else:
        if not synthesis_path.exists():
          debate_buildClaudePrompts()
            if not agents:
            if stage == 'r1':
              _debate_build_r1(stage, debate_dir, plugin_root, agents, agent_filter)
                for agent in agents:
                  if agent_filter and agent_filter != agent:
                  if render.exists():
                    common/scripts/jot/render_template.py::main() [blocking subproc]
                      (no resolvable entry function in target)
                  else:
            if stage == 'r2':
              _debate_build_r2(debate_dir, agents, agent_filter)
                for agent in agents:
                  if agent_filter and agent_filter != agent:
                  for other in others:
            if stage == 'synthesis':
              _debate_build_synthesis(debate_dir, agents)
                for agent in agents:
                for agent in agents:
            else:
        debate_buildClaudeCmd()
          bgPermissions_loadClaude('debate')
            _loadSection(tool, 'claude', bundle_path, log_file)
              _seedIfNeeded(bundle_path)
                _resolveInstalled()
                  if not plugin_data:
                _resolveBundle(bundle_path)
                  if bundle_path is not None:
                  if not plugin_root:
                claude_seedPermissions(str(installed_path), str(bundle_json), str(bundle_sha), str(prior_sha_path), log_file, 'bg_agent_permissions')
                  if not Path(default).is_file() or not Path(default_sha_file).is_file():
                    claude_permseedLog(f'bundled permissions default missing at {default} - cannot seed', log_file, log_prefix)
                      if not log_file:
                      try:
                      except OSError:
                  _util_readFirstToken(default_sha_file)
                  if not Path(installed).is_file():
                    claude_permseedLog(f'seeded {installed} from bundled default (sha={current_default_sha})', log_file, log_prefix)
                      if not log_file:
                      try:
                      except OSError:
                  try:
                    _util_sha256File(installed)
                  except OSError:
                  _util_readFirstToken(prior_sha_file)
                  if installed_sha == current_default_sha:
                  if prior_sha and installed_sha == prior_sha:
                    claude_permseedLog(f'upgraded {installed} to new bundled default (was {prior_sha}, now {current_default_sha})', log_file, log_prefix)
                      if not log_file:
                      try:
                      except OSError:
                  if prior_sha != current_default_sha:
                    claude_permseedLog(f'{installed} is user-edited; bundled default updated - diff manually. installed_sha={installed_sha} prior_sha={prior_sha} current_default_sha={current_default_sha}', log_file, log_prefix)
                      if not log_file:
                      try:
                      except OSError:
              try:
              except (OSError, json.JSONDecodeError):
                _resolveBundle(bundle_path)
                  if bundle_path is not None:
                  if not plugin_root:
              if key not in data:
              if worker not in section:
            _substituteTemplates(section.get('allow', []), env)
              for item in items:
            if extra_allow:
          claude_buildCmd(settings_file, allow_json, hooks_json_file, cwd, repo_root)
            for extra in extra_dirs:
        debate_claimSession()
          for n in range(1, _MAX_SESSIONS + 1):
            if tmux_runner(argv) == 0:
        if not session:
          hookjson_emitBlock('/debate: could not claim debate-<N> session (1000 already in use)')
        for cmd in _tmux_set:
          [blocking subproc] <unresolved argv>
        tmux select-pane [blocking subproc]
        [FaF subproc] <unresolved argv>
        terminal_spawnIfNeeded()
          if not session:
          _terminal_listTmuxClients(session)
            try:
              tmux list-clients [blocking subproc]
              if result.returncode != 0: -> return ''
            except (OSError, FileNotFoundError): -> return ''
          if clients.strip(): -> return 0
          if sys.platform == 'darwin':
            if shutil.which('osascript') is None:
              _terminal_appendAdvisory(log_file, log_prefix, session) -> return 0
                if not log_file or log_file == '/dev/null':
                try:
                except OSError:
            _terminal_buildOsascript(session, maximize)
            try:
              osascript [FaF subproc]
              try:
              finally:
            except (OSError, subprocess.SubprocessError): -> return 0
          _terminal_appendNonDarwinAdvisory(log_file, log_prefix, session) -> return 0
            if not log_file or log_file == '/dev/null':
            try:
            except OSError:
        hookjson_emitBlock(f'/debate {verb} ({agents_str}) -> {rel}/synthesis.md (~10-30 min). View: tmux attach -t {session}')
    debate_abortMain()  # _PROMPT_DISPATCH['/debate-abort']
      debate_initHookContext()
        if not plugin_root:
        if not plugin_data:
        try:
        except OSError:
        try:
          if not isinstance(payload, dict):
        except json.JSONDecodeError:
        try:
          git -C [blocking subproc]
        except (OSError, FileNotFoundError):
      hookjson_checkRequirements('debate-abort', 'jq', 'tmux')
        for cmd in cmds:
          if shutil.which(cmd) is None:
            hookjson_installHint(cmd)
        if not missing: -> return None
        hookjson_emitBlock(f'{prefix} needs: {joined} - install and retry.')
      if not transcript_path:
        hookjson_emitBlock('/debate-abort: no transcript_path in hook payload') -> return 0
      if not repo_root:
        hookjson_emitBlock('/debate-abort requires a git repository') -> return 0
      if debates_root.is_dir():
        for entry in debates_root.iterdir():
          if not entry.is_dir():
          if not marker.is_file():
          try:
          except OSError:
          if stored != transcript_path:
          if ts > best_ts:
      if best is None:
        hookjson_emitBlock('/debate-abort: no debate found in this conversation') -> return 0
      if debate_anyLiveLock(str(best)):
        debate_liveSession(str(best))
          for lock_path in lock_files: -> return ''
            try:
            except OSError:
            if not match:
            try:
              tmux display-message [blocking subproc]
            except OSError:
            if proc.returncode != 0:
            if session:
        hookjson_emitBlock(f'/debate-abort: debate is running. to force-kill: tmux kill-session -t {live}') -> return 0
      hookjson_emitBlock(f'/debate-abort: deleted {best}') -> return 0
    todo_main()  # _PROMPT_DISPATCH['/todo']
      if not plugin_data:
      try:
      except OSError:
      if '"/todo' not in hook_input: -> return 0
      try:
      except OSError:
      hookjson_checkRequirements('todo', 'jq', 'python3', 'tmux', 'claude')
        for cmd in cmds:
          if shutil.which(cmd) is None:
            hookjson_installHint(cmd)
        if not missing: -> return None
        hookjson_emitBlock(f'{prefix} needs: {joined} - install and retry.')
      try:
      except (ValueError, TypeError): -> return 0
      if not isinstance(payload, dict): -> return 0
      if not isinstance(prompt, str): -> return 0
      if prompt != '/todo' and (not prompt.startswith('/todo ')): -> return 0
      if idea.startswith(' '):
      _git_get_repo_root(cwd)
        try:
          git -C [blocking subproc]
          if result.returncode != 0: -> return ''
        except (OSError, subprocess.SubprocessError): -> return ''
      if not repo_root: -> return 0
        hookjson_emitBlock("todo requires a git repository. Run 'git init' in your project root.") -> return 0
    todo_listMain()  # _PROMPT_DISPATCH['/todo-list']
      _util_resolvePluginRoot()
        if env_root:
      if '"/todo-list' not in raw: -> return 0
      hookjson_checkRequirements('todo-list', 'jq', 'python3')
        for cmd in cmds:
          if shutil.which(cmd) is None:
            hookjson_installHint(cmd)
        if not missing: -> return None
        hookjson_emitBlock(f'{prefix} needs: {joined} - install and retry.')
      try:
      except json.JSONDecodeError: -> return 0
      if not isinstance(prompt_raw, str): -> return 0
      if prompt != '/todo-list' and (not prompt.startswith('/todo-list ')): -> return 0
      _git_repoRoot(cwd)
        try:
          git -C [blocking subproc]
        except (FileNotFoundError, OSError): -> return None
        if result.returncode != 0: -> return None
      if not repo_root:
        hookjson_emitBlock('todo-list: not a git repository.') -> return 0
      if not todos_dir.is_dir():
        hookjson_emitBlock('No Todos/ folder found in this project.') -> return 0
      skills/todo-list/scripts/format_open_todos.py::main() [blocking subproc]
        (no resolvable entry function in target)
      if not formatted:
        hookjson_emitBlock('No open TODOs.')
      else: -> return 0
        hookjson_emitBlock(formatted)
```

## Logic paths

Total distinct paths: 103.

### PATH P1  [dispatch_main via _ARGV_DISPATCH['jot-session-start']]

```
  dispatch_main()
  if argv:
  jot_sessionStart()  # _ARGV_DISPATCH['jot-session-start']
  if not input_file or not tmpdir_inv:
  return 0
```

### PATH P2  [dispatch_main via _ARGV_DISPATCH['jot-session-start']]

```
  dispatch_main()
  if argv:
  jot_sessionStart()  # _ARGV_DISPATCH['jot-session-start']
  if not tmux_target:
  return 0
```

### PATH P3  [dispatch_main via _ARGV_DISPATCH['jot-session-start']]

```
  dispatch_main()
  if argv:
  jot_sessionStart()  # _ARGV_DISPATCH['jot-session-start']
  if tmux_waitForClaudeReadiness(tmux_target) != 0:
  return 1
```

### PATH P4  [dispatch_main via _ARGV_DISPATCH['jot-session-start']]

```
  dispatch_main()
  if argv:
  jot_sessionStart()  # _ARGV_DISPATCH['jot-session-start']
  return 0
```

### PATH P5  [dispatch_main via _ARGV_DISPATCH['jot-session-end']]

```
  dispatch_main()
  if argv:
  jot_sessionEnd()  # _ARGV_DISPATCH['jot-session-end']
  if not tmpdir_inv:
  return 0
```

### PATH P6  [dispatch_main via _ARGV_DISPATCH['jot-session-end']]

```
  dispatch_main()
  if argv:
  jot_sessionEnd()  # _ARGV_DISPATCH['jot-session-end']
  if not re.match('^(/tmp/jot\\.|/private/tmp/jot\\.)', tmpdir_inv):
  return 0
```

### PATH P7  [dispatch_main via _ARGV_DISPATCH['jot-session-end']]

```
  dispatch_main()
  if argv:
  jot_sessionEnd()  # _ARGV_DISPATCH['jot-session-end']
  return 0
```

### PATH P8  [dispatch_main via _ARGV_DISPATCH['jot-stop']]

```
  dispatch_main()
  if argv:
  jot_stop()  # _ARGV_DISPATCH['jot-stop']
  if not input_file or not tmpdir_inv or (not state_dir):
  return 0
```

### PATH P9  [dispatch_main via _ARGV_DISPATCH['jot-stop']]

```
  dispatch_main()
  if argv:
  jot_stop()  # _ARGV_DISPATCH['jot-stop']
  if not tmux_target:
  return 0
```

### PATH P10  [dispatch_main via _ARGV_DISPATCH['jot-stop']]

```
  dispatch_main()
  if argv:
  jot_stop()  # _ARGV_DISPATCH['jot-stop']
  return 0
```

### PATH P11  [dispatch_main via _ARGV_DISPATCH['scan-open-todos']]

```
  dispatch_main()
  if argv:
  todo_scanOpen()  # _ARGV_DISPATCH['scan-open-todos']
  if not todos_dir.is_dir():
  return []
```

### PATH P12  [dispatch_main via _ARGV_DISPATCH['scan-open-todos']]

```
  dispatch_main()
  if argv:
  todo_scanOpen()  # _ARGV_DISPATCH['scan-open-todos']
  return open_paths
```

### PATH P13  [dispatch_main via _ARGV_DISPATCH['todo-launcher']]

```
  dispatch_main()
  if argv:
  todo_launcher()  # _ARGV_DISPATCH['todo-launcher']
  if not session_id:
  return 1
```

### PATH P14  [dispatch_main via _ARGV_DISPATCH['todo-launcher']]

```
  dispatch_main()
  if argv:
  todo_launcher()  # _ARGV_DISPATCH['todo-launcher']
  if not idea:
  return 1
```

### PATH P15  [dispatch_main via _ARGV_DISPATCH['todo-launcher']]

```
  dispatch_main()
  if argv:
  todo_launcher()  # _ARGV_DISPATCH['todo-launcher']
  if not pending_file_path:
  return 1
```

### PATH P16  [dispatch_main via _ARGV_DISPATCH['todo-launcher']]

```
  dispatch_main()
  if argv:
  todo_launcher()  # _ARGV_DISPATCH['todo-launcher']
  if not pending_file.is_file():
  return 1
```

### PATH P17  [dispatch_main via _ARGV_DISPATCH['todo-launcher']]

```
  dispatch_main()
  if argv:
  todo_launcher()  # _ARGV_DISPATCH['todo-launcher']
  except Exception:
  return 1
```

### PATH P18  [dispatch_main via _ARGV_DISPATCH['todo-launcher']]

```
  dispatch_main()
  if argv:
  todo_launcher()  # _ARGV_DISPATCH['todo-launcher']
  except Exception:
  return 1
```

### PATH P19  [dispatch_main via _ARGV_DISPATCH['todo-launcher']]

```
  dispatch_main()
  if argv:
  todo_launcher()  # _ARGV_DISPATCH['todo-launcher']
  return 0
```

### PATH P20  [dispatch_main via _ARGV_DISPATCH['todo-stop']]

```
  dispatch_main()
  if argv:
  todo_stop()  # _ARGV_DISPATCH['todo-stop']
  if not input_file or not tmpdir_inv or (not state_dir):
  return 0
```

### PATH P21  [dispatch_main via _ARGV_DISPATCH['todo-stop']]

```
  dispatch_main()
  if argv:
  todo_stop()  # _ARGV_DISPATCH['todo-stop']
  if not tmux_target:
  return 0
```

### PATH P22  [dispatch_main via _ARGV_DISPATCH['todo-stop']]

```
  dispatch_main()
  if argv:
  todo_stop()  # _ARGV_DISPATCH['todo-stop']
  return 0
```

### PATH P23  [dispatch_main via _ARGV_DISPATCH['todo-session-start']]

```
  dispatch_main()
  if argv:
  todo_sessionStart()  # _ARGV_DISPATCH['todo-session-start']
  if not input_file or not tmpdir_inv:
  return 0
```

### PATH P24  [dispatch_main via _ARGV_DISPATCH['todo-session-start']]

```
  dispatch_main()
  if argv:
  todo_sessionStart()  # _ARGV_DISPATCH['todo-session-start']
  if not tmux_target:
  return 0
```

### PATH P25  [dispatch_main via _ARGV_DISPATCH['todo-session-start']]

```
  dispatch_main()
  if argv:
  todo_sessionStart()  # _ARGV_DISPATCH['todo-session-start']
  if tmux_waitForClaudeReadiness(tmux_target) != 0:
  return 1
```

### PATH P26  [dispatch_main via _ARGV_DISPATCH['todo-session-start']]

```
  dispatch_main()
  if argv:
  todo_sessionStart()  # _ARGV_DISPATCH['todo-session-start']
  return jot_sendPrompt(tmux_target, input_file)
```

### PATH P27  [dispatch_main via _ARGV_DISPATCH['todo-session-end']]

```
  dispatch_main()
  if argv:
  todo_sessionEnd()  # _ARGV_DISPATCH['todo-session-end']
  if not any((tmpdir_inv.startswith(prefix) for prefix in _VALID_PREFIXES)):
  return
```

### PATH P28  [dispatch_main via _ARGV_DISPATCH['plate-summary-stop']]

```
  dispatch_main()
  if argv:
  plate_summaryStop()  # _ARGV_DISPATCH['plate-summary-stop']
  if not repo or not branch or (not output_file):
  return 0
```

### PATH P29  [dispatch_main via _ARGV_DISPATCH['plate-summary-stop']]

```
  dispatch_main()
  if argv:
  plate_summaryStop()  # _ARGV_DISPATCH['plate-summary-stop']
  if not Path(output_file).is_file():
  return 0
```

### PATH P30  [dispatch_main via _ARGV_DISPATCH['plate-summary-stop']]

```
  dispatch_main()
  if argv:
  plate_summaryStop()  # _ARGV_DISPATCH['plate-summary-stop']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  _cmd_set_plate_summary() [via plate_cli.py argv 'set-plate-summary']
  if len(argv) != 3:
  return 'plate: usage: set-plate-summary <repo> <branch> <summary-file>'
```

### PATH P31  [dispatch_main via _ARGV_DISPATCH['plate-summary-stop']]

```
  dispatch_main()
  if argv:
  plate_summaryStop()  # _ARGV_DISPATCH['plate-summary-stop']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  _cmd_set_plate_summary() [via plate_cli.py argv 'set-plate-summary']
  return f'plate: summary written ({new_tip[:8]} on {branch}-plate)'
```

### PATH P32  [dispatch_main via _ARGV_DISPATCH['plate-summary-stop']]

```
  dispatch_main()
  if argv:
  plate_summaryStop()  # _ARGV_DISPATCH['plate-summary-stop']
  return 0
```

### PATH P33  [dispatch_main via _ARGV_DISPATCH['plate-summary-watch']]

```
  dispatch_main()
  if argv:
  plate_summaryWatch()  # _ARGV_DISPATCH['plate-summary-watch']
  while elapsed < timeout:
  if _ready():
  return 0
```

### PATH P34  [dispatch_main via _ARGV_DISPATCH['plate-summary-watch']]

```
  dispatch_main()
  if argv:
  plate_summaryWatch()  # _ARGV_DISPATCH['plate-summary-watch']
  return 1
```

### PATH P35  [dispatch_main via _ARGV_DISPATCH['debate-tmux-orchestrator']]

```
  dispatch_main()
  if argv:
  debate_tmuxOrchestrator()  # _ARGV_DISPATCH['debate-tmux-orchestrator']
  return 0
```

### PATH P36  [dispatch_main via _ARGV_DISPATCH['jot-diag-collect']]

```
  dispatch_main()
  if argv:
  jot_collectDiagnostics()  # _ARGV_DISPATCH['jot-diag-collect']
  return out_path
```

### PATH P37  [dispatch_main]

```
  dispatch_main()
  if argv:
  if fn is not None:
  return int(rc) if rc is not None else 0
```

### PATH P38  [dispatch_main via _PROMPT_DISPATCH['/jot']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  jot_main()  # _PROMPT_DISPATCH['/jot']
  if '"/jot' not in hook_input:
  return 0
```

### PATH P39  [dispatch_main via _PROMPT_DISPATCH['/jot']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  jot_main()  # _PROMPT_DISPATCH['/jot']
  if tmux_requireVersion('2.9') != 0:
  return 0
```

### PATH P40  [dispatch_main via _PROMPT_DISPATCH['/jot']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  jot_main()  # _PROMPT_DISPATCH['/jot']
  if prompt != '/jot' and (not prompt.startswith('/jot ')):
  return 0
```

### PATH P41  [dispatch_main via _PROMPT_DISPATCH['/jot']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  jot_main()  # _PROMPT_DISPATCH['/jot']
  if not idea:
  return 0
```

### PATH P42  [dispatch_main via _PROMPT_DISPATCH['/jot']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  jot_main()  # _PROMPT_DISPATCH['/jot']
  if not repo_root:
  return 0
```

### PATH P43  [dispatch_main via _PROMPT_DISPATCH['/jot']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  jot_main()  # _PROMPT_DISPATCH['/jot']
  if os.environ.get('JOT_SKIP_LAUNCH') == '1':
  return 0
```

### PATH P44  [dispatch_main via _PROMPT_DISPATCH['/jot']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  jot_main()  # _PROMPT_DISPATCH['/jot']
  return 0
```

### PATH P45  [dispatch_main via _PROMPT_DISPATCH['/plate']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  if '/plate' not in raw_input:
  return 0
```

### PATH P46  [dispatch_main via _PROMPT_DISPATCH['/plate']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  except json.JSONDecodeError:
  return 0
```

### PATH P47  [dispatch_main via _PROMPT_DISPATCH['/plate']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  if not _PROMPT_RE_PLATE.match(prompt):
  return 0
```

### PATH P48  [dispatch_main via _PROMPT_DISPATCH['/plate']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  if not repo_root_raw:
  return 0
```

### PATH P49  [dispatch_main via _PROMPT_DISPATCH['/plate']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  else:
  return 0
```

### PATH P50  [dispatch_main via _PROMPT_DISPATCH['/plate']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  if not argv:
  return 0
```

### PATH P51  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['push']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_push()  # _DISPATCH['push']
  if len(argv) != 3:
  return 'plate: usage: push <convo_id> <transcript_path> <cwd>'
```

### PATH P52  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['push']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_push()  # _DISPATCH['push']
  if sha is None:
  return 'plate: no changes to stack'
```

### PATH P53  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['push']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_push()  # _DISPATCH['push']
  try:
  _spawn_summary(repo, branch, sha, transcript_path)
  scripts/jot_plugin_orchestrator.py::main() [FaF subproc]
  plate_summaryWatch() [via orchestrator argv 'plate-summary-watch']
  while elapsed < timeout:
  if _ready():
  return 0
```

### PATH P54  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['push']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_push()  # _DISPATCH['push']
  try:
  _spawn_summary(repo, branch, sha, transcript_path)
  scripts/jot_plugin_orchestrator.py::main() [FaF subproc]
  plate_summaryWatch() [via orchestrator argv 'plate-summary-watch']
  return 1
```

### PATH P55  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['push']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_push()  # _DISPATCH['push']
  return msg
```

### PATH P56  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['done']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_done()  # _DISPATCH['done']
  if len(argv) != 1:
  return 'plate: usage: done <repo>'
```

### PATH P57  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['done']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_done()  # _DISPATCH['done']
  return 'plate: replayed plate stack onto current branch'
```

### PATH P58  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['drop']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_drop()  # _DISPATCH['drop']
  if len(argv) != 1:
  return 'plate: usage: drop <repo>'
```

### PATH P59  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['drop']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_drop()  # _DISPATCH['drop']
  if result is None:
  return 'plate: no plate to drop'
```

### PATH P60  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['drop']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_drop()  # _DISPATCH['drop']
  return f'plate: dropped (saved patch at {result})'
```

### PATH P61  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['trash']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_trash()  # _DISPATCH['trash']
  if len(argv) < 1 or len(argv) > 2:
  return 'plate: usage: trash <repo> [--clean-wt]'
```

### PATH P62  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['trash']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_trash()  # _DISPATCH['trash']
  if result is None:
  return 'plate: no plate to trash'
```

### PATH P63  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['trash']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_trash()  # _DISPATCH['trash']
  return f'plate: trashed (saved patch at {result})'
```

### PATH P64  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['recycle']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_recycle()  # _DISPATCH['recycle']
  if len(argv) < 1 or len(argv) > 2:
  return 'plate: usage: recycle <repo> [--list|<session-dir-name>]'
```

### PATH P65  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['recycle']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_recycle()  # _DISPATCH['recycle']
  if len(argv) == 2:
  if argv[1] == '--list':
  return plate_lib.plate_recycle_list(repo)
```

### PATH P66  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['recycle']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_recycle()  # _DISPATCH['recycle']
  if result is None:
  return 'plate: nothing to recycle'
```

### PATH P67  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['recycle']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_recycle()  # _DISPATCH['recycle']
  return f'plate: recycled tip {result[:8]}'
```

### PATH P68  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['next']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_next()  # _DISPATCH['next']
  if len(argv) == 1:
  return plate_lib.plate_next(Path(argv[0]))
```

### PATH P69  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['next']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_next()  # _DISPATCH['next']
  if len(argv) == 2:
  return plate_lib.plate_next(Path(argv[0]), index=argv[1])
```

### PATH P70  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['next']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_next()  # _DISPATCH['next']
  return 'plate: usage: next <repo> [<index>]'
```

### PATH P71  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['show']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_show()  # _DISPATCH['show']
  return 'TODO'
```

### PATH P72  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['set-plate-summary']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_set_plate_summary()  # _DISPATCH['set-plate-summary']
  if len(argv) != 3:
  return 'plate: usage: set-plate-summary <repo> <branch> <summary-file>'
```

### PATH P73  [dispatch_main via _PROMPT_DISPATCH['/plate'] via _DISPATCH['set-plate-summary']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  _cmd_set_plate_summary()  # _DISPATCH['set-plate-summary']
  return f'plate: summary written ({new_tip[:8]} on {branch}-plate)'
```

### PATH P74  [dispatch_main via _PROMPT_DISPATCH['/plate']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  if handler is None:
  return 0
```

### PATH P75  [dispatch_main via _PROMPT_DISPATCH['/plate']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  try:
  common/scripts/plate/plate_cli.py::main() [blocking subproc]
  main()
  return 0
```

### PATH P76  [dispatch_main via _PROMPT_DISPATCH['/plate']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  plate_main()  # _PROMPT_DISPATCH['/plate']
  return 0
```

### PATH P77  [dispatch_main via _PROMPT_DISPATCH['/debate']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  debate_launch()  # _PROMPT_DISPATCH['/debate']
  (fallthrough)
```

### PATH P78  [dispatch_main via _PROMPT_DISPATCH['/debate-retry']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  debate_retryMain()  # _PROMPT_DISPATCH['/debate-retry']
  if not transcript_path:
  return 0
```

### PATH P79  [dispatch_main via _PROMPT_DISPATCH['/debate-retry']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  debate_retryMain()  # _PROMPT_DISPATCH['/debate-retry']
  if not repo_root:
  return 0
```

### PATH P80  [dispatch_main via _PROMPT_DISPATCH['/debate-retry']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  debate_retryMain()  # _PROMPT_DISPATCH['/debate-retry']
  if best is None:
  return 0
```

### PATH P81  [dispatch_main via _PROMPT_DISPATCH['/debate-retry']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  debate_retryMain()  # _PROMPT_DISPATCH['/debate-retry']
  if (best / 'synthesis.md').exists():
  return 0
```

### PATH P82  [dispatch_main via _PROMPT_DISPATCH['/debate-retry']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  debate_retryMain()  # _PROMPT_DISPATCH['/debate-retry']
  if debate_anyLiveLock(str(best)):
  return 0
```

### PATH P83  [dispatch_main via _PROMPT_DISPATCH['/debate-retry']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  debate_retryMain()  # _PROMPT_DISPATCH['/debate-retry']
  return 0
```

### PATH P84  [dispatch_main via _PROMPT_DISPATCH['/debate-abort']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  debate_abortMain()  # _PROMPT_DISPATCH['/debate-abort']
  if not transcript_path:
  return 0
```

### PATH P85  [dispatch_main via _PROMPT_DISPATCH['/debate-abort']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  debate_abortMain()  # _PROMPT_DISPATCH['/debate-abort']
  if not repo_root:
  return 0
```

### PATH P86  [dispatch_main via _PROMPT_DISPATCH['/debate-abort']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  debate_abortMain()  # _PROMPT_DISPATCH['/debate-abort']
  if best is None:
  return 0
```

### PATH P87  [dispatch_main via _PROMPT_DISPATCH['/debate-abort']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  debate_abortMain()  # _PROMPT_DISPATCH['/debate-abort']
  if debate_anyLiveLock(str(best)):
  return 0
```

### PATH P88  [dispatch_main via _PROMPT_DISPATCH['/debate-abort']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  debate_abortMain()  # _PROMPT_DISPATCH['/debate-abort']
  return 0
```

### PATH P89  [dispatch_main via _PROMPT_DISPATCH['/todo']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  todo_main()  # _PROMPT_DISPATCH['/todo']
  if '"/todo' not in hook_input:
  return 0
```

### PATH P90  [dispatch_main via _PROMPT_DISPATCH['/todo']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  todo_main()  # _PROMPT_DISPATCH['/todo']
  except (ValueError, TypeError):
  return 0
```

### PATH P91  [dispatch_main via _PROMPT_DISPATCH['/todo']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  todo_main()  # _PROMPT_DISPATCH['/todo']
  if not isinstance(payload, dict):
  return 0
```

### PATH P92  [dispatch_main via _PROMPT_DISPATCH['/todo']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  todo_main()  # _PROMPT_DISPATCH['/todo']
  if not isinstance(prompt, str):
  return 0
```

### PATH P93  [dispatch_main via _PROMPT_DISPATCH['/todo']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  todo_main()  # _PROMPT_DISPATCH['/todo']
  if prompt != '/todo' and (not prompt.startswith('/todo ')):
  return 0
```

### PATH P94  [dispatch_main via _PROMPT_DISPATCH['/todo']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  todo_main()  # _PROMPT_DISPATCH['/todo']
  if not repo_root:
  return 0
```

### PATH P95  [dispatch_main via _PROMPT_DISPATCH['/todo']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  todo_main()  # _PROMPT_DISPATCH['/todo']
  return 0
```

### PATH P96  [dispatch_main via _PROMPT_DISPATCH['/todo-list']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  todo_listMain()  # _PROMPT_DISPATCH['/todo-list']
  if '"/todo-list' not in raw:
  return 0
```

### PATH P97  [dispatch_main via _PROMPT_DISPATCH['/todo-list']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  todo_listMain()  # _PROMPT_DISPATCH['/todo-list']
  except json.JSONDecodeError:
  return 0
```

### PATH P98  [dispatch_main via _PROMPT_DISPATCH['/todo-list']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  todo_listMain()  # _PROMPT_DISPATCH['/todo-list']
  if not isinstance(prompt_raw, str):
  return 0
```

### PATH P99  [dispatch_main via _PROMPT_DISPATCH['/todo-list']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  todo_listMain()  # _PROMPT_DISPATCH['/todo-list']
  if prompt != '/todo-list' and (not prompt.startswith('/todo-list ')):
  return 0
```

### PATH P100  [dispatch_main via _PROMPT_DISPATCH['/todo-list']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  todo_listMain()  # _PROMPT_DISPATCH['/todo-list']
  if not repo_root:
  return 0
```

### PATH P101  [dispatch_main via _PROMPT_DISPATCH['/todo-list']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  todo_listMain()  # _PROMPT_DISPATCH['/todo-list']
  if not todos_dir.is_dir():
  return 0
```

### PATH P102  [dispatch_main via _PROMPT_DISPATCH['/todo-list']]

```
  dispatch_main()
  for (prefix, fn) in sorted(_PROMPT_DISPATCH, key=lambda p: -len(p[0])):
  todo_listMain()  # _PROMPT_DISPATCH['/todo-list']
  return 0
```

### PATH P103  [dispatch_main]

```
  dispatch_main()
  return 0
```

## Unresolvable subprocess invocations

- `common/scripts/tmux_lib.py:294`  `subprocess.run(cmd, capture_output=True, text=True, check=False)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/util_lib.py:32`  `subprocess.run(cmd, cwd=cwd, env=full_env, text=True, capture_output=capture, check=check)`
- `common/scripts/debate_lib.py:1918`  `subprocess.run(cmd, stderr=subprocess.DEVNULL)`
- `common/scripts/debate_lib.py:1951`  `subprocess.Popen(daemon_cmd, stdout=orch_log_handle, stderr=orch_log_handle, stdin=subprocess.DEVNULL, start_new_session=True, env=daemon_env)`

## Indirect dispatch (manual review)

_(none)_

